# Using tools

## From chat to agent

1. The access to tools
2. since the model can take action and observe the output – we call it: `ReAct` agent
3. Access the database, call API, search the web, call other agents 
